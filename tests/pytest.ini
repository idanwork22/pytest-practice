[pytest]
python_files = test_*
python_classes = *Tests
python_functions = test_*

markers =
    engine: All car engine tests
    smoke : All critical smoke tests
    body: All car body tests
    entertainment: All tests covering the entertainment system
    ui: All UI checks

# pytest -v (show all the tests)
# pytest -s (show the prints)
# pytest -m mark1
# pytest -m "mark1 and mark2"
#pytest -m "mark1 or mark2"
# pytest -m "not mark3"
#pytest --html="results.html" (to see tests result on a html file)
# pytest -rxXs # show extra info on xfailed, xpassed and skipped tests
#  pytest --env dev -v -rs